# Server Configuration
PORT=3001
NODE_ENV=development

# JWT Configuration (IMPORTANT: Use a strong secret in production!)
JWT_SECRET=your_super_secure_jwt_secret_key_here_change_in_production

# Pinata IPFS Configuration
PINATA_JWT=your_pinata_jwt_token_here
PINATA_GATEWAY=gateway.pinata.cloud

# Database Configuration (for future production use)
# DATABASE_URL=mongodb://localhost:27017/blue-carbon-registry
# or
# DATABASE_URL=postgres://user:password@localhost:5432/blue_carbon_registry

# Email Configuration (for notifications - optional)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_email_password
EMAIL_FROM=Blue Carbon Registry <noreply@bluecarbonregistry.org>

# Blockchain Configuration
CONTRACT_ADDRESS=your_deployed_contract_address_here
BLOCKCHAIN_RPC_URL=https://rpc-mumbai.maticvigil.com

# File Upload Limits
MAX_FILE_SIZE=52428800
MAX_FILES_PER_UPLOAD=10

# Security Settings
BCRYPT_ROUNDS=10
TOKEN_EXPIRY=24h
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Example values (DO NOT USE THESE IN PRODUCTION!):
# JWT_SECRET=myVerySecureJWTSecret2024!@#$%
# PINATA_JWT=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Get your Pinata JWT from: https://app.pinata.cloud/keys
# Generate a strong JWT secret using: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"